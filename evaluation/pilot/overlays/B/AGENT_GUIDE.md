# Guía mínima para esta tarea

- Objetivo: completar `render_report` en `reporting.py`.
- Límite: no modificar archivos de `tests/` ni instalar dependencias.
- Validación: `python -m unittest discover -s tests -t .`.
- No publiques, no hagas commit y no uses red.
