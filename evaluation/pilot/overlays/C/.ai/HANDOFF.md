# Handoff

## Objetivo

Completar `render_report` en `reporting.py`.

## Estado al interrumpir

La API y su docstring ya existen. La función todavía lanza
`NotImplementedError`; no se han modificado las pruebas.

## Límites

- No modificar `tests/`.
- No instalar dependencias, usar red, hacer commit ni publicar.

## Validación y siguiente paso

Implementar la función y ejecutar `python -m unittest discover -s tests -t .`.
